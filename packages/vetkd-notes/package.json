{
  "name": "shipstone-labs-ic-vetkd-notes",
  "scripts": {
    "build": "cargo build",
    "build:release": "cargo build --release",
    "doc": "cargo doc --no-deps --document-private-items && cp -r ../../target/wasm32-unknown-unknown/doc/ ../../docs/"
  }
}
