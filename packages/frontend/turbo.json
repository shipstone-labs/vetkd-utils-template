{
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["public/build/**"]
    },
    "build:release": {
      "outputs": ["public/build/**"],
      "env": ["NODE_ENV"]
    },
    "dev": {}
  }
}
